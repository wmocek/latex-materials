Place your slide image here and name it exactly `mile_marker.png` (or adjust the filename in `PREZ_ŻORY.tex`).

Suggested name: mile_marker.png

You can copy the attached image into this folder. On macOS you can run:

cp /path/to/your/downloaded/image.jpg /Users/wojciechmocek/Downloads/LATEX/images/mile_marker.png

Then recompile your LaTeX document (e.g., pdflatex or latexmk).